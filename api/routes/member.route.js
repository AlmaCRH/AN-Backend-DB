const router = require('express').Router()

const {  } = require('../controllers/member.controller')


router.get('/', )
router.get('/:id', )
router.post('/', )
router.put('/:id', )
router.delete('/:id', )

module.exports = router
